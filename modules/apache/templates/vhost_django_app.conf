<VirtualHost *:80>
        ServerName <%= name %>
        # Serve static content directly
        DocumentRoot  /dev/null

        WSGIScriptAlias / /usr/local/lib/wsgi/<%= name %>.wsgi

        <Location />
            Allow from all
        </Location>
</VirtualHost>


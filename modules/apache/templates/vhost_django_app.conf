<VirtualHost *:80>
        ServerName <%= name %>
        # Serve static content directly
        DocumentRoot  /dev/null

        WSGIScriptAlias / <%= wsgi_dir%>/<%= name %>.wsgi

        <Location />
            Allow from all
        </Location>
</VirtualHost>

